#include "main.h"

/**
 * mul - multiplies 2 integers
 * @a: checks the first integer
 * @b: checks the second integer
 * Return: the result of the multiplication
 */

int mul(int a, int b)
{
return (a * b);
}
